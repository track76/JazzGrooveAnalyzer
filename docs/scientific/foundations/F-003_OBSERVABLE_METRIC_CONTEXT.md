# F-003 — Observable Metric Context

Before analysing individual events, the JGA builds the
Observable Metric Context.

The Observable Metric Context identifies:

- meter
- local tempo
- ensemble pulse
- metric contributors
- musical functions

All subsequent analysis depends on this context.
