# Single-root bootstrap recovery contract — PI authorized 2026-09-22

Additive infrastructure decision clarifying the output-path contract of locked AD-026; no scientific or runtime change. Canonical scientific/project records remain source authority. Root JGA_BOOTSTRAP.md is the sole CURRENT recovery entry, deterministically generated from docs/project/BOOTSTRAP_SOURCES.json and the canonical documents it selects. Never manually prepend project state to either bootstrap. Update canonical sources and regenerate root; source records prevail on conflict, which must be reported before dependent work continues.

The artifacts/JGA_BOOTSTRAP.md path is a NON-AUTHORITATIVE redirect only, retained because historical links use it. No current startup procedure loads it; no active generator writes it. Historical snapshots carry date/hash provenance and are not live authorities. Existing frozen scripts/manifests remain unchanged and must run only in their recorded historical context, never as current state synchronizers. Historical byte checks resolve through the migration preservation record, not through the redirect.

AGENTS.md is deliberately brought under version control for recovery across clones. Both former bootstrap byte streams are preserved in docs/scientific/rfc/JGA_SINGLE_ROOT_BOOTSTRAP_MIGRATION_20260922/PRE_MIGRATION/. Existing scientific results and the preceding closure freeze are unchanged; their old documentation hashes describe that historical state.

Use `python tools/bootstrap.py --recovery-only` for root-only generation, without tests, scientific execution, documentation updaters or archive creation. The older full export command retains its side effects and is not a startup requirement. JGA_CONTEXT.zip remains a docs-only archive, supplied alongside root bootstrap; JGA_REPOSITORY.zip remains committed HEAD, not an uncommitted working-tree snapshot. Recovery must consult Git for operational commit identity; generated bootstrap has no self-referential commit ID.
