# Synthetic numerical execution readiness

This resource-only benchmark was authorized after production-harness acceptance. It does not evaluate real Drum observations. Frozen benchmark source was hashed before its single execution; it imports the unchanged accepted evaluator, LRU cache, Writer and certified environment. No scientific PASS criterion, parameter tuning or new numerical claim was introduced.

63 synthetic events use binary64 producer t=i/10, s=(i+1)/67, i=0..62, retaining JSON tokens, bits, hex and exact ratios. Queries use u=0, L=2*t[31] then 2*t[62], with 1056 rational resource probes f=j/1057 per support. These are resource probes, not a production search grid. Exactly 32 and 63 events accumulate respectively, including the boundary term. Every accumulated exponential misses the bounded cache; no assumption of warm-cache speedup enters projection. Synthetic IDs are deliberately explicit and long; all 63 construction IDs are retained for every period-probe provenance. No magnitude/phase interpretation or selection was performed.

measurement.json contains exact descriptive resource counters. Full-population runtime uses the slower per-query section plus amortized close overhead, treating all 4,080,384 queries as 63-event workloads. The separate numerical estimate scales evaluator time by 133,195,392 accumulations. Neither is a guaranteed bound on other inputs or machine load.

The operational budget is 10 hours per execution (20 for execution plus replay), over four times the measured conservative extrapolation. Plan 1 GiB worker RSS. Storage reserves are 256 GiB per execution, plus 128 GiB extra headroom: 640 GiB free required. These are prospective operational allowances, not scientific thresholds or proven universal bounds. Streaming can monitor capacity and stop safely without pruning if reserves prove insufficient. Preregistration must define such incomplete-run handling; no automatic repair or overwrite.

The compact maximum observed row (28,054 bytes) projects to 114471092736 bytes. Metadata is additionally covered by the storage reserve. The synthetic period manifest retains 63 construction IDs per coordinate, exercising more lineage than the 1,953 unique generating pairs spread across the target's 1,056 periods. Window manifests remain incremental and only 3,864 target windows are needed. The bound on query/index buffers and caches is inherited unchanged. This benchmark does not establish byte-identical equivalence for a new scientific domain; it only measures existing validated computations and packaging.

Bulk benchmark artifacts remain external at the path in measurement.json. No real-data directories are created. No real execution is authorized by this readiness decision. Freeze the complete real input and execution plan next, then obtain PI authorization.
