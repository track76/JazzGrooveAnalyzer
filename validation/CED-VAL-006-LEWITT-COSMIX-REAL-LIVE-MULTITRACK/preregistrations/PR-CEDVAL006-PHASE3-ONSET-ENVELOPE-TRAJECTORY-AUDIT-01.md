# PR-CEDVAL006-PHASE3-ONSET-ENVELOPE-TRAJECTORY-AUDIT-01

Status: **PREREGISTERED — NOT EXECUTED**

This read-only audit captures raw and detector-normalized onset-envelope
trajectories at the frozen Phase-3 candidate coordinates. The fixed symmetric
neighborhood is eight frames on each side: 17 frames total, or approximately
plus/minus 92.88 ms. This is the smallest symmetric window containing the
unchanged detector's full adaptive-mean window and was fixed before population
computation.

The audit replays the native local-maximum, adaptive-threshold and greedy-wait
conditions without changing them. Primary transition classifications use the
fixed precedence in the adjacent JSON. Original Bass authority is used only
for inherited retrospective population labels.

Diagnostic plots use the earliest coordinate in each observed mechanism and
B/C2/E class, with deterministic deduplication and styling. The prospective
gate is fixed at 75% explained in each transition population plus a single
mechanism covering 40% of the combined population. No production rule is
defined or implemented.
