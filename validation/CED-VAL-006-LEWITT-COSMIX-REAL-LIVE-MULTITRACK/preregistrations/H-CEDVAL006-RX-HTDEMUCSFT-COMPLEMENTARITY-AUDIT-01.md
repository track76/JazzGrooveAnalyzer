# H-CEDVAL006 RX versus htdemucs_ft Complementarity Audit 01

This protocol freezes a read-only identity-set audit of the already accepted
original Bass matches from htdemucs_ft and RX11. No audio, detector, matching,
or JGA operation is authorized.

The four populations are the exhaustive set partition defined in the adjacent
JSON protocol. Timing is summarized only from the frozen selected matches.
Original Bass identity and time are retrospective comparison authority and
must not be used to construct an oracle or production fusion rule.

Classification and the separate prospective-fusion gate are frozen in the
machine-readable protocol before cross-separator population computation.
